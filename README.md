# youme-content

Content store for **Youme** — images, markdown, and other static assets used across the product.

This repo is not an application. Put source content here (copy, illustrations, screenshots, docs drafts) so it can be versioned and reused by `youme-app`, `youme-api`, and `youme-dashboard`.